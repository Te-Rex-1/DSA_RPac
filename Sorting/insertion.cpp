#include<iostream>
using namespace std;
main(){

bool swapped=false
// insertion sort
for (int i = 0; i < count; i++)
{
    for (int j = 0; j < count-1; j++)
    {
        /* code */
        if (arr[current]>arr[next])
        {
            /* code */
            swap(arr[current],arr[next]);
            swap=true;
        }
        
    }
    
}



return 0;
}